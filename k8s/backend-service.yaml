apiVersion: v1
kind: Service
metadata:
  name: calcify-backend-service
  labels:
    app: calcify-backend
spec:
  type: NodePort
  selector:
    app: calcify-backend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30081
