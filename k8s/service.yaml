apiVersion: v1
kind: Service
metadata:
  name: calcify-service
  labels:
    app: calcify
spec:
  type: NodePort
  selector:
    app: calcify
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
